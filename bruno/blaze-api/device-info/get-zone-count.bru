meta {
  name: Get Zone Count
  type: http
  seq: 4
}

docs {
  Command:  GET ZONE.COUNT
  Response: +ZONE.COUNT 4
            *GET ZONE.COUNT

  Returns 2, 4, or 8 depending on device variant.
  Use this as the first call to auto-detect device type.
}
